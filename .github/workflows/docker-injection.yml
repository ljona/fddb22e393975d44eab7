name: Test docker invocation command injection

on: [ push ]

jobs:
    change-working-dir:
        runs-on: ubuntu-latest
        steps:
            - name: job_1
              working-directory: /tmp
              run: |
                  ls
    invoke-docker:
        runs-on: self-hosted
        steps:
            - name: job_2
              run: |
                  docker run --rm alpine:latest /bin/sh -c 'echo "something"'
