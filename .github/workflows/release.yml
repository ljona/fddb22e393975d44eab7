name: Test releasing

on: [ release ]

jobs:
  do_release:
    if:  ${{ github.ref == 'v[0-9]+.*' }}
    runs-on: ubuntu-latest
    steps:
      - name: Something stupid
      run: |
          echo "This does not mean anything"
          ls $HOME
